// Quick Quiz: Use the library function to calculate the area of a square with side a. 
#include <stdio.h>
#include <math.h>

int main(){
    float a = 5;
    printf("The saquare of %.2f is = %.2f\n", a, pow(a, 2));
    return 0;
}